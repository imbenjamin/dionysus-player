## Checklist

- [ ] If this PR adds/changes any user-facing string (`Text`, `String(localized:)`, etc.),
      I opened the project in Xcode.app and built once (Cmd+B) to sync
      `Localizable.xcstrings`, and its diff is included in this PR — see
      `CLAUDE.md`'s Localization section.
